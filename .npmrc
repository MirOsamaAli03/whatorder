# Workspace packages are linked as directory junctions on Windows, which do not
# require elevation. pnpm was tried first but links workspace packages with real
# symlinks, which Windows refuses without Developer Mode or admin rights.
install-strategy=hoisted
audit=false
fund=false
